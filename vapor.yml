id: 48646
name: partashare
environments:
    production:
        memory: 1024
        cli-memory: 512
        # runtime: 'php-8.2:al2'
        runtime: docker
        database: 'partage-db'
        storage: production-partagemavima
        build:
            - 'COMPOSER_MIRROR_PATH_REPOS=1 composer install --no-dev'
            - 'php artisan event:cache'
            - 'npm ci && npm run build && rm -rf node_modules'
        deploy:
            # - 'php artisan migrate --force'
   
